{
  "name": "ramsurrun-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install --prefix server",
    "install-codenames": "npm install --prefix codenames",
    "server": "npm run watch --prefix server",
    "codenames": "npm start --prefix codenames",
    "watch": "npm run server & npm run codenames",
    "deploy": "npm run build --prefix codenames && npm start --prefix server",
    "deploy-cluster": "npm run build --prefix codenames && npm run cluster --prefix server",
    "test": "npm test --prefix server && npm test --prefix codenames"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
